<Window x:Class="Calcoo.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Calcoo Settings" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}" Foreground="{DynamicResource TextForeground}"
        FontFamily="Segoe UI Variable, Segoe UI" FontSize="14"
        Button.Click="Settings_Button_Click">
    <Window.Resources>
        <Style x:Key="SettingsCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="Margin" Value="0,12,0,0"/>
        </Style>
        <Style x:Key="AccentButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentButtonBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentButtonForeground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="24,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SubtleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource CardBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="24,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Scrollable content -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" MaxHeight="500">
            <Grid Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="16"/>
                </Grid.RowDefinitions>

                <!-- Row 0-1: Autorelease (left) | Mode (right) -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Autorelease" Style="{StaticResource SectionHeader}" Margin="0,0,0,0"/>
                <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <CheckBox Content="arc button" Name="AutoreleaseArcButton" Margin="0,0,0,6"/>
                        <CheckBox Content="hyp button" Name="AutoreleaseHypButton"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="Mode" Style="{StaticResource SectionHeader}" Margin="0,0,0,0"/>
                <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <RadioButton GroupName="Mode" Name="ModeRpn" Content="RPN" Margin="0,0,0,6"/>
                        <RadioButton GroupName="Mode" Name="ModeAlgebraic" Content="Algebraic"/>
                    </StackPanel>
                </Border>

                <!-- Row 2-3: RPN Enter (left) | RPN Stack (right) -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="RPN Enter" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="3" Grid.Column="0" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <RadioButton GroupName="RpnEnter" Name="RpnEnterTraditional" Content="Traditional" Margin="0,0,0,6"/>
                        <RadioButton GroupName="RpnEnter" Name="RpnEnterHp28" Content="Hp28"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="2" Grid.Column="2" Text="RPN Stack" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="3" Grid.Column="2" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <RadioButton GroupName="RpnStack" Name="RpnStackInfinite" Content="Infinite" Margin="0,0,0,6"/>
                        <RadioButton GroupName="RpnStack" Name="RpnStackXyzt" Content="Xyzt"/>
                    </StackPanel>
                </Border>

                <!-- Row 4-5: Rounding (full width) -->
                <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Text="Rounding" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <CheckBox Content="Round output to" Name="RoundingOutputCheckBox" VerticalAlignment="Center"/>
                            <ComboBox Name="RoundingDigitsComboBox" Width="52" Margin="8,0,0,0" VerticalAlignment="Center"
                                      SelectionChanged="RoundingDigitsComboBox_SelectionChanged">
                                <ComboBox.Style>
                                    <Style TargetType="ComboBox">
                                        <Style.Triggers>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.4"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </ComboBox.Style>
                            </ComboBox>
                            <TextBlock Text="digits" Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <CheckBox Content="Truncate trailing zeros" Name="TruncateZerosCheckBox"/>
                    </StackPanel>
                </Border>

                <!-- Row 6-7: Paste Parsing (left) | Customize (right) -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="Paste Parsing" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="7" Grid.Column="0" Style="{StaticResource SettingsCard}">
                    <StackPanel>
                        <RadioButton GroupName="PasteParsing" Name="PasteParsingLocaleBased" Content="Locale-based" Margin="0,0,0,6"/>
                        <RadioButton GroupName="PasteParsing" Name="PasteParsingHeuristic" Content="Heuristic"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Row="6" Grid.Column="2" Text="Customize" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="7" Grid.Column="2" Style="{StaticResource SettingsCard}">
                    <Button Name="CustomButton" Content="&#x25C7; button" HorizontalAlignment="Left" Width="90"/>
                </Border>
            </Grid>
        </ScrollViewer>

        <!-- Button bar -->
        <Border Grid.Row="1" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="0,1,0,0" Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="SettingsOk" Content="OK" Style="{StaticResource AccentButtonStyle}" Margin="0,0,8,0"/>
                <Button Name="SettingsCancel" Content="Cancel" IsCancel="True" Style="{StaticResource SubtleButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
